service: aws-node-simple-http-endpoint
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs16.x

functions:
  currentTime:
    handler: handler.endpoint
    events:
      - httpApi:
          path: /time
          method: get
